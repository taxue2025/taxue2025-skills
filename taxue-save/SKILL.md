---
name: taxue-save
version: "3.2"
description: |
  踏雪状态存档——三种模式覆盖全生命周期。
  触发：/taxue-save、保存当前进度、继续上次的讨论、保存、存档、记一下、记住、留着、继续上次、接着做、接着聊、恢复。
  EN: "save this", "remember this", "continue from last time", "checkpoint", "resume our discussion".
---

# taxue-save：状态存档

> 复杂的问题不会在一次会话中完成。存档是为了下次能接着来。

---

## 三种模式

| 模式 | 功能 | 触发词 |
|------|------|--------|
| `--progress` | 保存问题解决中间状态 | 「保存进度」「存档」「记下现在到哪了」 |
| `--restore` | 恢复上次状态继续 | 「继续上次」「恢复」「上次到哪了」「接着做」 |
| `--decision` | 记录关键决策供复盘 | 「记一下这个决定」「记录决策」「复盘」 |

---

## 存档内容

```
# 存档：{项目/问题}
日期：{时间}
进度：{当前走到哪一步}
已验证：{已经确认的结论}
已否决：{已经排除的方向}
下一步：{接下来要做什么}
```

---

## 存档位置

默认放在 `~/.taxue/sessions/`，每个项目隔离。

---

## 记忆时效分层

存档和规律都需要标注时效等级，决定什么时候该检索、什么时候该遗忘。

### 时效等级（memory_tier）

| 等级 | 含义 | 检索规则 |
|------|------|---------|
| `permanent` | 认知规律、思维模型、方向判断 | 永远注入，不过期 |
| `periodic` | 偏好、习惯、阶段性目标 | 只注入最近一条，且用户明确提到相关话题时才注入 |
| `once` | 临时上下文、单次任务状态 | 会话结束即失效，不跨会话检索 |

存档时在 frontmatter 里标注：

```yaml
---
slug: default
timestamp: 2026-05-01T14:23:15+08:00
title: {标题}
source_skill: taxue-solve
status: in-progress
next_skill: taxue-business
memory_tier: periodic
---
```

### 自动判断规则

- 用户说的客观事实（做过什么、买过什么、收入多少）→ `permanent`
- 用户表达的主观倾向（喜欢什么、想做什么、不想做什么）→ `periodic`
- 当前会话的临时状态（正在分析的任务、待确认的假设）→ `once`

---

## 记忆检索

新会话开始时，skill 先扫 `~/.taxue/sessions/` 下所有存档：

- `memory_tier: permanent` — 话题匹配时注入上下文
- `memory_tier: periodic` — 只注入最近一条，且用户明确提到相关话题时才注入。旧偏好不如没有偏好
- `memory_tier: once` — 不检索

patterns.md 中 `半衰期: 永久` 的规律等同于 `permanent`，`半衰期: 周期` 等同于 `periodic`，`半衰期: 一次性` 等同于 `once`。

---

## 下一步建议（条件触发）

存档完成后，根据情况判断是否推荐下一步。**不是每次都推荐**，只在明确指向另一个 skill 时才说一句。

| 结果条件 | 推荐话术 |
|----------|---------|
| 恢复了上次的进度，接下来要继续解决 | 「进度恢复了，接着上次往下走。用 `/taxue-solve`。」 |
| 存了很多份档，发现反复在同类问题上打转 | 「你在重复同一个模式。值得固化成流程。用 `/taxue-build`。」 |

---

## 默会知识外化

存档时，检查本次会话是否满足以下任一条件：
- 用户纠正了 AI 的方向（不是执行细节，是方向）
- 用户表达了一个之前没出现过的偏好
- 发现了一个反复出现的模式（同类问题第 2 次出现）
- 解决了一个之前没遇到过的新问题类型

如果满足，提炼一条规律：
- 只写「在什么场景下 + 正确做法是什么」
- 不写具体事件、对话原文、文件路径
- 格式：一条规律，不超过 50 字
- 判断这条规律的时效等级，写入 `半衰期` 字段

写入 `~/.taxue/growth/patterns.md`，格式：

```markdown
### P{序号}：{规律标题}
- 把握：星级
- 出现次数：{N}
- 触发词：{触发场景关键词}
- 规律：{具体规律}
- 适用：{适用的子技能名，逗号分隔}
- 半衰期：{永久 | 周期 | 一次性}
```

如果 `~/.taxue/growth/` 目录不存在，先创建。

---

## DO NOT

- 从零创建新 skill：`skill-creator`
- 恢复状态后继续解决：`taxue-solve`（save 只管存取，不管解决）
- 多次存档后需要整理复盘：`taxue-build`（固化为工具）
- 将过期偏好当作当前偏好注入：`periodic` 等级的记忆只注入最近一条
---

*taxue-save v3.2*
